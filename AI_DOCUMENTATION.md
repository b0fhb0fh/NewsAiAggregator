# AI Documentation - NewsAiAggregator Bot

## 📋 Обзор проекта

**NewsAiAggregator** - это Telegram-бот для автоматического мониторинга каналов и фильтрации сообщений по релевантности с использованием Ollama AI. Бот пересылает релевантные сообщения в целевой канал.

### Основная функциональность
- Мониторинг указанных Telegram-каналов в реальном времени
- Анализ сообщений на соответствие заданным темам через Ollama API
- Автоматическая публикация релевантных сообщений в целевой канал
- Поддержка всех типов медиа (фото, видео, документы)
- Graceful shutdown при получении сигналов SIGINT/SIGTERM

---

## 🏗️ Архитектура

### Технологический стек
- **pyTelegramBotAPI (telebot)**: Отправка сообщений в целевой канал
- **Telethon**: Мониторинг каналов и получение сообщений
- **Ollama API**: Анализ релевантности сообщений
- **asyncio**: Асинхронная обработка событий
- **threading**: Параллельный запуск бота и Telethon клиента

### Архитектурные компоненты

```
┌─────────────────────────────────────────────────────────┐
│                    Main Process                          │
│  ┌──────────────────┐         ┌──────────────────┐     │
│  │  Telethon Client │         │  Telegram Bot    │     │
│  │   (asyncio)      │         │   (threading)    │     │
│  └────────┬─────────┘         └────────┬─────────┘     │
│           │                             │                │
│           │                             │                │
│  ┌────────▼─────────────────────────────▼──────────┐   │
│  │         Event Handler (handle_new_message)       │   │
│  └────────┬─────────────────────────────────────────┘   │
│           │                                             │
│  ┌────────▼─────────────────────────────────────────┐ │
│  │    Relevance Check (check_topic_relevance)       │ │
│  │              via Ollama API                        │ │
│  └────────┬─────────────────────────────────────────┘ │
│           │                                             │
│  ┌────────▼─────────────────────────────────────────┐ │
│  │    Send to Channel (send_message_to_channel)     │ │
│  │    ┌──────────────────────────────────────────┐  │ │
│  │    │  Try: forward_messages (Telethon)        │  │ │
│  │    │  Fallback: copy_message_to_channel       │  │ │
│  │    └──────────────────────────────────────────┘  │ │
│  └───────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

---

## 📁 Структура кода

### Основные функции и их назначение

#### 1. Инициализация и конфигурация (строки 33-73)

**Загрузка конфигурации:**
- Читает `config.json` при старте
- Валидирует обязательные параметры
- Устанавливает значения по умолчанию для опциональных параметров

**Обязательные параметры:**
- `TELEGRAM_BOT_TOKEN`: Токен бота для отправки сообщений
- `SUMMARY_CHANNEL_ID`: ID целевого канала для публикации
- `API_ID`, `API_HASH`: Credentials для Telethon
- `PHONE_NUMBER`: Номер телефона для авторизации Telethon
- `OLLAMA_URL`: URL API Ollama (обычно `http://localhost:11434/api/generate`)
- `OLLAMA_MODEL`: Название модели Ollama
- `INTEREST_TOPICS`: Список интересующих тем
- `CHANNELS_TO_MONITOR`: Список каналов для мониторинга

**Опциональные параметры:**
- `CHECK_INTERVAL`: Интервал проверки (по умолчанию 300 сек, но не используется в текущей реализации)
- `LOG_LEVEL`: Уровень логирования (по умолчанию "INFO")

#### 2. Обработка сигналов (строки 20-30)

**Graceful Shutdown:**
- Обработчики `SIGINT` и `SIGTERM`
- Устанавливает глобальный флаг `shutdown = True`
- Позволяет корректно завершить работу всех компонентов

#### 3. Проверка релевантности (строки 75-100)

**`check_topic_relevance(text: str) -> bool`**

**Логика работы:**
1. Формирует промпт для Ollama с темами из конфигурации
2. Отправляет POST-запрос к Ollama API
3. Парсит ответ (ожидает "Да" или "Нет")
4. Возвращает `True` если в ответе есть подстрока "да"

**Особенности:**
- Синхронная функция (блокирующая)
- Обрабатывает ошибки сети и API
- Логирует все запросы и ответы
- Возвращает `False` при ошибках (fail-safe)

**Пример промпта:**
```
Прочитай это сообщение и определи, относится ли оно к одной из этих тем: 
технологии, искусственный интеллект, наука. 
Ответь только 'Да' или 'Нет'.

Сообщение: [текст сообщения]
```

#### 4. Формирование ссылок (строки 102-132)

**`get_message_link(chat, message_id) -> str`**
- Создает прямую ссылку на сообщение в формате `t.me/channel/message_id`
- Обрабатывает каналы с username и без него
- Для каналов без username использует формат `t.me/c/{chat_id}/{message_id}`

**`format_source_info(chat, message_id) -> str`**
- Форматирует информацию об источнике с HTML-ссылкой
- Использует `@username` если доступен, иначе название канала или ID
- Возвращает строку в формате: `\n\n🔗 <a href="...">Источник: ...</a>`

#### 5. Отправка сообщений (строки 134-295)

**`send_message_to_channel(event)` - Основная функция отправки**

**Стратегия отправки:**
- Использует копирование через Bot API (`copy_message_to_channel`)
- Сообщения от бота отображаются как **непрочитанные** в целевом канале
- Это решает проблему автоматической пометки сообщений как прочитанных при использовании `forward_messages`

**`copy_message_to_channel(event)` - Копирование сообщений**

**Обработка типов сообщений:**

1. **Текстовые сообщения без медиа:**
   - Отправляет текст + ссылка на источник
   - Использует `bot.send_message()` с HTML parse_mode

2. **Медиа сообщения:**
   - Загружает медиа в `BytesIO` буфер
   - Определяет тип медиа:
     - `MessageMediaPhoto` → `bot.send_photo()`
     - `MessageMediaDocument` с `video/*` mime_type → `bot.send_video()`
     - Другие документы → `bot.send_document()`
   - Добавляет caption с текстом сообщения и ссылкой на источник

3. **Обработка ошибок:**
   - При ошибке загрузки медиа отправляет только текст
   - При неподдерживаемом типе медиа отправляет текст с предупреждением

**Важно:**
- Использует `asyncio.to_thread()` для вызова синхронных методов `bot.*`
- Всегда добавляет ссылку на источник в конце сообщения
- Закрывает буферы после использования
- **Сообщения отображаются как непрочитанные** (в отличие от forward через Telethon)

#### 6. Обработчик новых сообщений (строки 297-324)

**`handle_new_message(event)`**

**Поток обработки:**
1. Извлекает информацию о чате и сообщении
2. Определяет имя канала для логирования
3. Извлекает текст сообщения
4. Проверяет релевантность через `check_topic_relevance()`
   - Если текста нет, считает сообщение релевантным (`True`)
5. Если релевантно → вызывает `send_message_to_channel()`
6. Если не релевантно → логирует и пропускает

**Особенности:**
- Сообщения без текста автоматически считаются релевантными
- Все ошибки логируются с полным traceback

#### 7. Валидация каналов (строки 326-348)

**`validate_channels(channels) -> list`**

**Процесс валидации:**
1. Для каждого канала пытается получить entity через `client.get_entity()`
2. Разделяет каналы на валидные и невалидные
3. Логирует результаты валидации
4. Возвращает только валидные каналы

**Обрабатываемые ошибки:**
- `UsernameInvalidError`: Неверный формат username
- `UsernameNotOccupiedError`: Канал не существует
- `ValueError`: Неверный формат ID
- Другие исключения логируются как ошибки

#### 8. Запуск компонентов (строки 350-416)

**`run_telethon()` - Асинхронная функция Telethon**

**Процесс запуска:**
1. Запускает Telethon клиент с авторизацией по номеру телефона
2. Валидирует список каналов
3. Регистрирует обработчик `@client.on(events.NewMessage(chats=valid_channels))`
4. Входит в цикл ожидания флага `shutdown`
5. При завершении отключает клиент

**`run_bot()` - Синхронная функция бота**

- Запускает polling бота в бесконечном цикле
- Использует `bot.polling(none_stop=True, interval=1)`

**`main()` - Главная функция координации**

**Архитектура параллельного выполнения:**
1. Создает asyncio task для `run_telethon()`
2. Запускает `run_bot()` в отдельном потоке (daemon thread)
3. Основной цикл ожидает флаг `shutdown`
4. При завершении:
   - Отменяет Telethon task
   - Останавливает polling бота
   - Ждет завершения потока бота (timeout 2 сек)

---

## 🔄 Потоки выполнения

### Поток 1: Telethon (asyncio)
```
start() → validate_channels() → register_handler() → event_loop → handle_new_message()
                                                                    ↓
                                                          check_topic_relevance()
                                                                    ↓
                                                          send_message_to_channel()
```

### Поток 2: Telegram Bot (threading)
```
polling() → [idle, waiting for send_message calls from asyncio thread]
```

### Поток 3: Main (asyncio)
```
main() → create_task(telethon) → start_thread(bot) → wait_for_shutdown()
```

---

## ⚙️ Конфигурация

### Формат config.json

```json
{
  "TELEGRAM_BOT_TOKEN": "string",           // Обязательно
  "SUMMARY_CHANNEL_ID": "string|int",       // Обязательно (@channel или ID)
  "API_ID": "string|int",                   // Обязательно
  "API_HASH": "string",                     // Обязательно
  "PHONE_NUMBER": "string",                 // Обязательно (+1234567890)
  "OLLAMA_URL": "string",                   // Обязательно
  "OLLAMA_MODEL": "string",                 // Обязательно
  "INTEREST_TOPICS": ["array"],             // Обязательно
  "CHANNELS_TO_MONITOR": ["array"],         // Обязательно
  "CHECK_INTERVAL": 300,                    // Опционально (не используется)
  "LOG_LEVEL": "INFO"                       // Опционально
}
```

### Форматы ID каналов

Telethon принимает:
- Username: `@channelname`
- ID: `-1001234567890` (для приватных каналов)
- Entity object (после валидации)

---

## 🐛 Обработка ошибок

### Стратегии обработки

1. **Проверка релевантности:**
   - Ошибки сети → возвращает `False` (сообщение не отправляется)
   - Ошибки API → логируется, возвращает `False`

2. **Отправка сообщений:**
   - Ошибка загрузки медиа → отправка только текста
   - Неподдерживаемый тип медиа → отправка текста с предупреждением
   - Все ошибки логируются с полным traceback

3. **Валидация каналов:**
   - Невалидные каналы пропускаются
   - Если все каналы невалидны → программа завершается с ошибкой

4. **Graceful shutdown:**
   - Сигналы обрабатываются корректно
   - Все ресурсы освобождаются

---

## 🔍 Важные детали реализации

### 1. Смешанная архитектура (asyncio + threading)

**Проблема:** Telethon использует asyncio, pyTelegramBotAPI - синхронный.

**Решение:**
- Telethon работает в asyncio event loop
- Bot работает в отдельном потоке
- Вызовы синхронных методов бота оборачиваются в `asyncio.to_thread()`

### 2. Обработка медиа

**Особенности:**
- Медиа загружается в память (`BytesIO`)
- Буфер проверяется на пустоту перед отправкой
- Буфер закрывается после использования
- MIME type определяется для правильной отправки видео

### 3. Формирование ссылок на сообщения

**Логика:**
- Для каналов с username: `t.me/{username}/{message_id}`
- Для каналов без username: `t.me/c/{chat_id_without_minus100}/{message_id}`
- Chat ID для приватных каналов имеет формат `-100...`, первые 4 символа удаляются

### 4. Проверка релевантности сообщений без текста

**Текущее поведение:** Сообщения без текста считаются релевантными (`True`).

**Причина:** Медиа-сообщения могут быть релевантными по визуальному контенту, но анализ текста невозможен.

### 5. Непрочитанные сообщения

**Реализация:** Используется копирование через Bot API вместо `forward_messages` из Telethon.

**Причина:** При использовании `forward_messages` сообщения автоматически помечаются как прочитанные в целевом канале. Копирование через Bot API гарантирует, что сообщения отображаются как непрочитанные, что удобно для пользователя.

**Компромисс:** При копировании теряется прямая связь с оригинальным сообщением (нет метки "Forwarded from"), но добавляется ссылка на источник в тексте сообщения.

### 6. Логирование

**Уровни:**
- `INFO`: Основные события (запуск, получение сообщений, отправка)
- `WARNING`: Невалидные каналы, ошибки обработки медиа
- `ERROR`: Критические ошибки с traceback
- `DEBUG`: Пропущенные нерелевантные сообщения

**Вывод:**
- В файл `bot.log`
- В консоль (stdout)

---

## 🚀 Точки расширения

### Потенциальные улучшения

1. **Кэширование проверок релевантности:**
   - Избежать повторных запросов к Ollama для одинаковых сообщений

2. **Обработка медиа без текста:**
   - Добавить анализ изображений через vision модели Ollama

3. **Rate limiting:**
   - Ограничение частоты запросов к Ollama API

4. **База данных:**
   - Сохранение истории обработанных сообщений
   - Предотвращение дубликатов

5. **Метрики и статистика:**
   - Подсчет обработанных сообщений
   - Статистика по каналам

6. **Конфигурация через переменные окружения:**
   - Альтернатива config.json для production

7. **Обработка CHECK_INTERVAL:**
   - Реализовать периодическую проверку вместо real-time мониторинга

8. **Поддержка других типов медиа:**
   - Аудио, голосовые сообщения, стикеры

---

## 📝 Зависимости

```
pyTelegramBotAPI  # Telegram Bot API wrapper
telethon          # Telegram MTProto API client
requests          # HTTP requests для Ollama API
feedparser        # (установлен, но не используется в коде)
beautifulsoup4    # (установлен, но не используется в коде)
```

---

## 🔐 Безопасность

### Важные моменты

1. **Конфиденциальные данные:**
   - `config.json` содержит токены и credentials
   - Не должен попадать в git (должен быть в `.gitignore`)
   - Используется `config.json.example` как шаблон

2. **Сессия Telethon:**
   - Файл `session_name.session` содержит авторизационные данные
   - Не должен попадать в git

3. **Обработка ошибок:**
   - Не раскрывает чувствительную информацию в логах
   - Graceful degradation при ошибках

---

## 🧪 Тестирование

### Рекомендуемые тесты

1. **Unit тесты:**
   - `check_topic_relevance()` с моками Ollama API
   - `get_message_link()` с разными форматами каналов
   - `format_source_info()` с разными типами чатов

2. **Integration тесты:**
   - Валидация каналов
   - Отправка сообщений разных типов
   - Обработка ошибок при отправке

3. **E2E тесты:**
   - Полный цикл: получение → проверка → отправка
   - Graceful shutdown

---

## 📚 Дополнительные заметки

### Неиспользуемые зависимости
- `feedparser` и `beautifulsoup4` установлены, но не используются в коде
- Возможно, планировались для парсинга RSS или HTML контента

### Неиспользуемый параметр
- `CHECK_INTERVAL` определен в конфигурации, но не используется
- Текущая реализация использует real-time мониторинг через события Telethon

### Совместимость версий
- Python 3.7+ (используется `asyncio.run()`)
- Telethon 1.42.0+
- pyTelegramBotAPI 4.29.1+

---

## 🎯 Быстрый старт для ИИ

При работе с этим проектом:

1. **Для добавления функциональности:**
   - Изучите существующие функции перед изменением
   - Учитывайте смешанную архитектуру (asyncio + threading)
   - Используйте существующие паттерны обработки ошибок

2. **Для отладки:**
   - Проверьте логи в `bot.log`
   - Убедитесь, что Ollama запущен и доступен
   - Проверьте валидность каналов в конфигурации

3. **Для оптимизации:**
   - Обратите внимание на синхронные вызовы Ollama API
   - Рассмотрите кэширование результатов проверки релевантности
   - Оптимизируйте загрузку медиа (возможно, streaming)

---

*Документация создана для облегчения работы ИИ с проектом. Последнее обновление: 2025*

